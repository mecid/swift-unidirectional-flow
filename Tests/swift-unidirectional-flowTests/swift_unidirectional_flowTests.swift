import XCTest
@testable import swift_unidirectional_flow

final class swift_unidirectional_flowTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(swift_unidirectional_flow().text, "Hello, World!")
    }
}
